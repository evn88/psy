import styles from './Testimonials.module.css';

export const Testimonials = () => {
  return (
    <section className={styles.testimonials}>
      <div className="container">{/* TODO: результаты клиентов — карточки с отзывами */}</div>
    </section>
  );
};
